.python_job:
  image: python:3.12-slim
  interruptible: true
  timeout: 10 minutes
  before_script:
    - python --version
  retry:
    max: 1
    when:
      - runner_system_failure
      - stuck_or_timeout_failure
